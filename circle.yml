machine:
  environment:
    RAILS_ENV: test
    RACK_ENV: test

dependencies:
  post:
    - rake assets:precompile

test:
  override:
    - bundle exec rake test:
        parallel: true
        files:
          - test/**/*_test.rb
