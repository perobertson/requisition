tasks:
  restart:
    action: "Restart Application"
    description: "Sometimes needed if you the application to restart but don't want to ship any new code."
    steps:
      - heroku ps:restart --app space-dolphins

review:
  checks:
    - heroku status

deploy:
  override: []
